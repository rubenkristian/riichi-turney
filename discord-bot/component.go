package discordbot

import "github.com/disgoorg/disgo/events"

func (db *DiscordBot) ComponentSelectGroup(event *events.ComponentInteractionCreate) {
	// event.Client().Rest().AddMemberRole(*event.GuildID, )
}

func (db *DiscordBot) ComponentSelectTime(event *events.ComponentInteractionCreate) {
	// event.Client().Rest().AddMemberRole(*event.GuildID, )
}
